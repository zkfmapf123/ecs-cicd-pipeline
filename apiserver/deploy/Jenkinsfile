pipeline {
    agent any

    stages {
        stage('Push ECR') {
            steps {
                dir("apiserver") {
                    script {
                        sh "ls"
                    }
                }
            }
        }

        stage("Deploy ECS") {
            steps {
                dir("apiserver/deploy") {
                    sh "ls"
                }
            }
        }
    }
}
